# ✅ نظام الإحصائيات الديناميكي - دليل الاستخدام

## 📋 نظرة عامة

تم إصلاح لوحة التحكم لعرض إحصائيات ديناميكية تبدأ من 0 للمستخدمين الجدد وتتحدث بناءً على النشاط الفعلي.

## 🎯 نظام النقاط

### كيفية كسب النقاط:
- ✅ **إكمال مهمة**: +10 نقاط
- 📚 **إكمال درس**: +15 نقطة
- 🏆 **كسب شارة**: نقاط إضافية (تختلف حسب الشارة)

### عرض النقاط:
- يتم عرض النقاط في بطاقة "Total Points" في لوحة التحكم
- يتم تحديث العرض تلقائياً عند إكمال أي نشاط
- النقاط محفوظة في localStorage

## 🏆 نظام الشارات

### الشارات المتاحة (9 شارات):

1. **🎯 First Step** - إكمال أول مهمة (+10 نقاط)
2. **✅ Task Starter** - إكمال 5 مهام (+25 نقطة)
3. **✅ Task Master** - إكمال 10 مهام (+50 نقطة)
4. **📚 Learner** - إكمال 5 دروس (+25 نقطة)
5. **🎓 Scholar** - إكمال 20 درس (+100 نقطة)
6. **🧠 Brain Teaser** - إكمال 3 اختبارات (+50 نقطة)
7. **💯 Century** - الوصول إلى 100 نقطة
8. **⭐ Rising Star** - الوصول إلى 500 نقطة
9. **🔥 Week Warrior** - سلسلة 7 أيام (+30 نقطة)

### كيفية كسب الشارات:
- يتم فحص الشارات تلقائياً بعد كل نشاط
- عند كسب شارة، تظهر رسالة: "🏆 [اسم الشارة] earned!"
- الشارات المكتسبة تظهر في لوحة التحكم
- الشارات المقفلة تظهر باللون الرمادي

## 📊 الإحصائيات المتتبعة

يتم تتبع الإحصائيات التالية في `data.stats`:
- `tasksCompleted`: عدد المهام المكتملة
- `lessonsCompleted`: عدد الدروس المكتملة
- `testsCompleted`: عدد الاختبارات المكتملة
- `totalPoints`: إجمالي النقاط المكتسبة
- `lastActivity`: آخر نشاط
- `streak`: سلسلة الأيام المتتالية

## 🔄 ترحيل البيانات

### للمستخدمين الحاليين:
- إذا كانت النقاط = 1250 (القيمة القديمة الثابتة)
- يتم إعادة تعيين النقاط إلى 0
- يتم إعادة تعيين الشارات إلى []
- يتم إنشاء كائن stats جديد

### للمستخدمين الجدد:
- النقاط تبدأ من 0
- لا توجد شارات
- stats فارغة

## 💻 الاستخدام البرمجي

### إضافة نقاط:
```javascript
Store.addPoints(10, 'Task completed');
```

### تحديث الإحصائيات:
```javascript
Store.updateStats('task');  // أو 'lesson' أو 'test'
```

### فحص الشارات:
```javascript
const newBadges = Store.checkAndAwardBadges();
if (newBadges.length > 0) {
    // عرض إشعارات
}
```

### الحصول على النقاط الحالية:
```javascript
const points = Store.getPoints();
```

### الحصول على الشارات:
```javascript
const { earned, locked } = Store.getBadges();
```

## 🧪 الاختبار

### خطوات الاختبار:
1. افتح DevTools → Application → Local Storage
2. احذف `daily_planner_data`
3. أعد تحميل الصفحة
4. ✅ تحقق: النقاط = 0، الشارات = 0
5. أكمل مهمة
6. ✅ تحقق: النقاط = 10
7. ✅ تحقق: شارة "🎯 First Step" مكتسبة
8. ✅ تحقق: النقاط = 20 (10 + 10 للشارة)

## 📝 ملاحظات

- جميع البيانات محفوظة في localStorage
- التحديثات فورية وتلقائية
- رسائل console.log للتتبع:
  - `✨ +10 points: Task completed (Total: 10)`
  - `📊 Stats updated: task completed`
  - `🏆 Badge earned: 🎯 First Step (+10 bonus points)`

## 🎨 واجهة المستخدم

### لوحة التحكم تعرض:
1. **Total Points**: النقاط الحالية (ديناميكية)
2. **Pending Tasks**: المهام المعلقة (ديناميكية)
3. **Badges Earned**: عدد الشارات (ديناميكية)

### كل بطاقة تحتوي على:
- العنوان
- القيمة الديناميكية
- نص توضيحي

## 🔧 الملفات المعدلة

1. **`js/store.js`**:
   - تغيير `points: 1250` إلى `points: 0`
   - تغيير `badges: [...]` إلى `badges: []`
   - إضافة `stats` object
   - إضافة methods: `addPoints()`, `updateStats()`, `checkAndAwardBadges()`, `getBadges()`
   - إضافة data migration

2. **`js/app.js`**:
   - تحديث `toggleTask()` لإضافة نقاط
   - تحديث `completeLesson()` لإضافة نقاط
   - إضافة إشعارات الشارات

3. **`js/ui.js`**:
   - تحديث `renderHome()` لعرض البيانات الديناميكية
   - إضافة نصوص توضيحية للبطاقات
